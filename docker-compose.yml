version: '3.8'

services:
  elia-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: elia-platform
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/elia.db
      - HOST=0.0.0.0
      - PORT=8000
      - DEBUG=false
      - UPLOAD_DIR=static/uploads
      - MAX_UPLOAD_SIZE=52428800
    volumes:
      - ./data:/app/data
      - ./static/uploads:/app/static/uploads
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Опциональный PostgreSQL (раскомментируйте для использования)
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: elia-postgres
  #   environment:
  #     - POSTGRES_USER=elia
  #     - POSTGRES_PASSWORD=elia_password
  #     - POSTGRES_DB=elia
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped

# volumes:
#   postgres_data:


# Новые функции версии 1.0.2

Дата релиза: 2 декабря 2025

---

## 📋 Содержание

1. [Версионирование статических файлов](#версионирование-статических-файлов)
2. [Улучшенная обработка камеры](#улучшенная-обработка-камеры)
3. [Тестовые данные в настройках](#тестовые-данные-в-настройках)
4. [Исправления Docker](#исправления-docker)

---

## 🎨 Версионирование статических файлов

### Проблема

При обновлении версии приложения браузеры кэшировали старые CSS и JS файлы, что требовало ручной очистки кэша.

### Решение

Добавлено автоматическое версионирование всех статических файлов через query string с версией приложения:

```html
<!-- До -->
<link rel="stylesheet" href="/static/css/styles.css">
<script src="/static/js/main.js"></script>

<!-- После -->
<link rel="stylesheet" href="/static/css/styles.css?v=1.0.2">
<script src="/static/js/main.js?v=1.0.2"></script>
```

### Как это работает

1. Версия берётся из `app/config.py` (`version: str = "1.0.0"`)
2. Передаётся во все шаблоны через контекст
3. Добавляется к каждому статическому файлу автоматически

### Преимущества

- ✅ Браузеры автоматически загружают новые файлы при обновлении версии
- ✅ Не требуется ручная очистка кэша
- ✅ Простое обновление версии в одном месте

### Обновление версии

Просто измените версию в `app/config.py`:

```python
version: str = "1.0.3"  # Новая версия
```

Все статические файлы автоматически получат новую версию в URL.

---

## 📷 Улучшенная обработка камеры

### Проблема

При работе по HTTP браузеры блокировали доступ к камере без понятных сообщений об ошибках.

### Решение

Добавлена детальная обработка ошибок доступа к камере с информативными сообщениями и инструкциями.

### Новые возможности

1. **Детальные сообщения об ошибках:**
   - `NotAllowedError` → "Доступ к камере запрещён"
   - `NotFoundError` → "Камера не найдена"
   - `NotReadableError` → "Камера занята"
   - `SecurityError` → "Требуется HTTPS соединение"

2. **Инструкции для HTTP:**
   - Chrome: `chrome://flags` → "Insecure origins treated as secure"
   - Firefox: `about:config` → `media.getusermedia.insecure.enabled = true`
   - Альтернатива: использование кнопки "Изображение"

3. **Кнопка "Повторить попытку":**
   - Позволяет быстро повторить запрос доступа к камере

### Использование

При ошибке доступа к камере пользователь увидит:
- Понятное сообщение об ошибке
- Инструкции по решению проблемы
- Кнопку для повторной попытки

### Документация

Подробная документация: [`docs/CAMERA_ACCESS_FIX.md`](CAMERA_ACCESS_FIX.md)

---

## ⚙️ Тестовые данные в настройках

### Проблема

Текст стенограммы для mock-транскрипции хранился только в файле `data/talk.md`, что неудобно для редактирования.

### Решение

Добавлен новый функционал в настройках для управления тестовыми данными.

### Новые возможности

1. **Пункт "Тестовые данные" в настройках:**
   - Малозаметный пункт внизу страницы настроек
   - Открывает модальное окно для редактирования

2. **Сохранение в БД:**
   - Данные сохраняются в таблице `test_data`
   - Ключ: `transcription_text`
   - Автоматическая загрузка при первом запуске из файла

3. **API endpoints:**
   - `GET /api/test-data` — получить тестовые данные
   - `POST /api/test-data` — сохранить тестовые данные

4. **Автоматический fallback:**
   - Если данных нет в БД, используется файл `data/talk.md`
   - При первом запуске данные автоматически загружаются в БД

### Использование

1. Откройте страницу "Настройки"
2. Прокрутите вниз до пункта "Тестовые данные"
3. Нажмите "Редактировать тестовые данные"
4. Отредактируйте текст в модальном окне
5. Нажмите "Сохранить"

### Структура БД

```sql
CREATE TABLE test_data (
    id INTEGER PRIMARY KEY,
    key VARCHAR(50) UNIQUE DEFAULT 'transcription_text',
    content TEXT,
    updated_at DATETIME,
    created_at DATETIME
);
```

---

## 🐳 Исправления Docker

### Проблемы

1. База данных не монтировалась в `docker-compose.hub.yml`
2. Неправильный порт (8000 вместо 80)
3. Проблемы с правами доступа при первом запуске

### Решения

1. **Добавлен volume для БД:**
   ```yaml
   volumes:
     - ./elia.db:/app/elia.db
   ```

2. **Исправлен порт:**
   ```yaml
   ports:
     - "${PORT:-80}:80"  # Было: 8000:8000
   ```

3. **Автоматическая инициализация:**
   - При первом запуске создаются все таблицы
   - Тестовые данные автоматически загружаются из файла в БД
   - Правильные права доступа устанавливаются автоматически

### Проверка при деплое

После деплоя проверьте:

```bash
# Структура БД
docker exec elia-platform python3 -c "
import sqlite3
conn = sqlite3.connect('/app/elia.db')
cursor = conn.cursor()
cursor.execute('SELECT name FROM sqlite_master WHERE type=\"table\"')
print([t[0] for t in cursor.fetchall()])
"

# Тестовые данные
curl http://localhost/api/test-data

# Права доступа
ls -lah /opt/elia-platform/elia.db
```

---

## 🔧 Технические детали

### Изменённые файлы

- `templates/*.html` — добавлено версионирование статических файлов
- `static/js/patient-card.js` — улучшена обработка камеры
- `templates/settings.html` — добавлен UI для тестовых данных
- `app/models.py` — добавлена модель `TestData`
- `app/crud.py` — добавлены CRUD функции для тестовых данных
- `app/main.py` — добавлены API endpoints и инициализация
- `app/api/audio.py` — обновлён mock-transcription для использования БД
- `docker-compose.hub.yml` — исправлены volumes и порт
- `Dockerfile.production` — улучшена работа с БД

### Миграции БД

Новая таблица `test_data` создаётся автоматически при первом запуске через `init_db()`.

### Обратная совместимость

- ✅ Старые данные сохраняются
- ✅ Fallback на файл `talk.md` работает
- ✅ Все существующие функции работают как прежде

---

## 📝 Рекомендации

1. **При обновлении версии:**
   - Измените версию в `app/config.py`
   - Пересоберите Docker образ
   - Задеплойте на сервер

2. **Для работы камеры:**
   - Рекомендуется настроить HTTPS (см. [`CAMERA_ACCESS_FIX.md`](CAMERA_ACCESS_FIX.md))
   - Или используйте инструкции для HTTP в браузере

3. **Для редактирования тестовых данных:**
   - Используйте интерфейс в настройках
   - Или редактируйте напрямую через API

---

## 🐛 Известные ограничения

1. **Камера на HTTP:**
   - Требует специальных настроек браузера
   - Рекомендуется использовать HTTPS в production

2. **Тестовые данные:**
   - Хранятся только один набор данных (ключ `transcription_text`)
   - Для расширения функционала можно добавить дополнительные ключи

---

## 📚 Связанная документация

- [CAMERA_ACCESS_FIX.md](CAMERA_ACCESS_FIX.md) — настройка доступа к камере
- [DOCKER_GUIDE.md](DOCKER_GUIDE.md) — работа с Docker
- [UBUNTU_DEPLOYMENT.md](UBUNTU_DEPLOYMENT.md) — развёртывание на сервере

---

**Вернуться к [оглавлению документации](README.md)**


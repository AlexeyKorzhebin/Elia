# 🔒 Настройка доступа к камере

## Проблема

Браузеры блокируют доступ к камере для сайтов, работающих по HTTP. Это политика безопасности - камера доступна только через HTTPS.

## ⚠️ Временное решение (только для разработки)

### Chrome / Edge

1. Откройте `chrome://flags/` (или `edge://flags/`)
2. Найдите `Insecure origins treated as secure`
3. Добавьте ваш адрес: `http://43.245.224.114`
4. Перезапустите браузер

### Firefox

1. Откройте `about:config`
2. Найдите `media.getusermedia.insecure.enabled`
3. Установите значение `true`
4. Перезапустите браузер

### Safari

1. Safari → Настройки → Веб‑сайты → Камера
2. Разрешите доступ для `http://43.245.224.114`

---

## ✅ Правильное решение: Настройка HTTPS

### Вариант 1: Использование домена с Let's Encrypt ✅ (Настроено для elia.su)

**Статус:** HTTPS успешно настроен для домена `elia.su`

1. **DNS настроен:**
   ```
   A запись: elia.su → 43.245.224.114 ✅
   ```

2. **Nginx и Certbot установлены:**
   ```bash
   # Установка выполнена:
   apt update
   apt install -y nginx certbot python3-certbot-nginx
   ```

3. **Конфигурация Nginx:**
   ```bash
   # Конфигурация находится в: /etc/nginx/sites-enabled/elia-platform
   # Проксирование на Docker контейнер: http://127.0.0.1:8000
   ```

4. **SSL сертификат получен:**
   ```bash
   # Сертификат: /etc/letsencrypt/live/elia.su/
   # Действителен до: 2026-03-03
   # Автоматическое обновление: Настроено через certbot.timer
   ```

5. **Docker контейнер настроен:**
   ```yaml
   # docker-compose.yml обновлён:
   ports:
     - "127.0.0.1:8000:80"  # Только локальный доступ
   ```

**Доступ:**
- ✅ HTTPS: https://elia.su
- ✅ HTTP: http://elia.su (автоматический редирект на HTTPS)
- ✅ Камера: Работает через HTTPS без дополнительных настроек!

**Для настройки на другом домене:**
```bash
ssh root@43.245.224.114

# Установка Nginx и Certbot
apt update
apt install -y nginx certbot python3-certbot-nginx

# Создание конфигурации Nginx
cat > /etc/nginx/sites-available/elia-platform << 'EOF'
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
EOF

# Активация конфигурации
ln -s /etc/nginx/sites-available/elia-platform /etc/nginx/sites-enabled/
rm -f /etc/nginx/sites-enabled/default
nginx -t
systemctl start nginx

# Получение SSL сертификата
certbot --nginx -d your-domain.com --email your@email.com --agree-tos --non-interactive --redirect

# Автоматическое обновление настроено автоматически через certbot.timer
```

### Вариант 2: Использование Cloudflare (бесплатно)

1. Зарегистрируйтесь на [Cloudflare](https://cloudflare.com)
2. Добавьте ваш домен
3. Настройте DNS записи
4. Включите SSL/TLS → Full (strict)
5. Cloudflare автоматически предоставит HTTPS

### Вариант 3: Self-signed сертификат (только для тестирования)

⚠️ **Внимание:** Браузеры будут показывать предупреждение о безопасности!

```bash
ssh root@43.245.224.114

# Установка Nginx
apt update && apt install -y nginx openssl

# Генерация самоподписанного сертификата
mkdir -p /etc/nginx/ssl
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout /etc/nginx/ssl/elia.key \
  -out /etc/nginx/ssl/elia.crt \
  -subj "/C=RU/ST=State/L=City/O=Organization/CN=43.245.224.114"

# Конфигурация Nginx с SSL
cat > /etc/nginx/sites-available/elia-platform << 'EOF'
server {
    listen 80;
    server_name 43.245.224.114;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name 43.245.224.114;
    
    ssl_certificate /etc/nginx/ssl/elia.crt;
    ssl_certificate_key /etc/nginx/ssl/elia.key;
    
    location / {
        proxy_pass http://127.0.0.1:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
EOF

# Активация
ln -s /etc/nginx/sites-available/elia-platform /etc/nginx/sites-enabled/
rm -f /etc/nginx/sites-enabled/default
nginx -t
systemctl restart nginx

# Открытие порта 443
ufw allow 443/tcp
```

---

## 🔍 Проверка работы

После настройки HTTPS:

1. Откройте сайт по адресу `https://your-domain.com`
2. Проверьте, что в адресной строке есть замок 🔒
3. Попробуйте открыть камеру - доступ должен работать

---

## 📝 Дополнительные настройки браузера

После настройки HTTPS, разрешения на камеру можно настроить в браузере:

### Chrome / Edge
- Нажмите на замок в адресной строке
- Выберите "Настройки сайта"
- Разрешите доступ к камере

### Firefox
- Нажмите на замок в адресной строке
- Выберите "Разрешения"
- Разрешите доступ к камере

### Safari
- Safari → Настройки → Веб‑сайты → Камера
- Разрешите для вашего домена

---

## ⚠️ Важные замечания

1. **HTTPS обязателен** для работы камеры в продакшене
2. Временные решения (флаги браузера) работают только локально
3. Self-signed сертификаты не подходят для продакшена
4. Рекомендуется использовать Let's Encrypt или Cloudflare

---

## 🆘 Если проблема сохраняется

1. Проверьте, что сайт открывается по HTTPS (замок в адресной строке)
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Убедитесь, что камера не используется другим приложением
4. Проверьте разрешения в настройках браузера


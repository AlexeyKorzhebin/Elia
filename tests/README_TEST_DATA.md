# Набор тестовых данных для проверки работоспособности приложения

Этот набор тестовых данных предназначен для проверки работоспособности приложения после изменений, без создания новых приёмов в базе данных.

## 🚀 Быстрый старт

```bash
# Быстрая проверка работоспособности
pytest tests/test_quick_check.py -v

# Все тесты функциональности
pytest tests/test_functionality.py -v

# Все тесты (без медленных)
pytest tests/ -v -m "not slow"
```

## 🗄️ Тестовая база данных

Все тесты используют **отдельную тестовую базу данных** `elia-test.db`, которая **не трогает продуктовую БД** `elia.db`.

📖 **Подробная документация**: см. [README_TEST_DB.md](README_TEST_DB.md)

### Настройка тестовой БД

- **Файл БД**: `elia-test.db` (в корне проекта)
- **Путь можно изменить**: установите переменную окружения `TEST_DATABASE_PATH`
- **Автоматическое создание**: БД создаётся автоматически при первом запуске тестов
- **Игнорируется Git**: файл `elia-test.db` уже добавлен в `.gitignore`

### Переменные окружения

```bash
# Изменить путь к тестовой БД
export TEST_DATABASE_PATH="custom-test.db"
pytest tests/ -v

# Очистить БД перед запуском тестов
export CLEAN_TEST_DB=1
pytest tests/ -v

# Очистить БД после завершения тестов
export CLEAN_TEST_DB_AFTER=1
pytest tests/ -v

# Можно использовать вместе
export CLEAN_TEST_DB=1 CLEAN_TEST_DB_AFTER=1
pytest tests/ -v
```

### Важно

- ✅ **Продуктовая БД не затрагивается** - тесты используют только `elia-test.db`
- ✅ **Данные сохраняются между запусками** - можно проверить состояние БД после тестов
- ✅ **Изоляция через фикстуры** - каждый тест получает свежие данные через `sample_patient`, `sample_appointment`
- ⚠️ **E2E тесты** (`test_e2e.py`) запускают реальный сервер и могут использовать продуктовую БД (если не настроена переменная окружения)

### Проверка тестовой БД

После запуска тестов можно проверить содержимое тестовой БД:

```bash
# Используя sqlite3
sqlite3 elia-test.db ".tables"
sqlite3 elia-test.db "SELECT * FROM patients;"
sqlite3 elia-test.db "SELECT * FROM appointments;"
```

## Структура файлов

- `test_data.py` - содержит все тестовые данные: транскрипции, анамнезы, запросы для API
- `test_functionality.py` - **основные pytest тесты** для проверки функциональности
- `test_quick_check.py` - **быстрые pytest тесты** для проверки работоспособности
- `test_usage_examples.py` - примеры использования тестовых данных (не pytest тесты)
- `quick_test.py` - скрипт для быстрой проверки (не pytest тесты)
- `README_TEST_DATA.md` - данная инструкция

## Быстрый старт

### 1. Использование готовых тестовых данных

```python
from tests.test_data import (
    get_test_transcription,
    get_test_anamnesis,
    get_test_medical_report,
    get_test_blood_pressure
)

# Получить транскрипцию для кардиологического случая
transcription = get_test_transcription("cardiovascular")

# Получить данные анамнеза
anamnesis = get_test_anamnesis("respiratory")

# Получить данные для медицинского отчёта
report = get_test_medical_report(1)

# Получить данные артериального давления
bp = get_test_blood_pressure(1)
```

### 2. Запуск тестов через pytest

```bash
# Все тесты функциональности
pytest tests/test_functionality.py -v

# Быстрая проверка работоспособности
pytest tests/test_quick_check.py -v

# Все тесты (включая существующие)
pytest tests/ -v

# Только быстрые тесты (без медленных)
pytest tests/ -v -m "not slow"

# Конкретный тест
pytest tests/test_functionality.py::TestTranscriptionWorkflow::test_update_transcription -v
```

## Доступные тестовые данные

### Транскрипции разговоров

Доступны транскрипции для различных медицинских случаев:

- `cardiovascular` - кардиологический случай (боли в сердце, гипертония)
- `respiratory` - респираторный случай (кашель, бронхит)
- `gastroenterology` - гастроэнтерологический случай (проблемы с желудком)
- `neurology` - неврологический случай (головные боли)
- `short` - короткий пример разговора

**Использование:**
```python
from tests.test_data import get_test_transcription

# Получить транскрипцию
text = get_test_transcription("cardiovascular")
```

### Данные анамнеза

Готовые структуры данных для заполнения медицинского отчёта:

- `cardiovascular` - анамнез для кардиологического случая
- `respiratory` - анамнез для респираторного случая
- `gastroenterology` - анамнез для гастроэнтерологического случая
- `neurology` - анамнез для неврологического случая

**Использование:**
```python
from tests.test_data import get_test_anamnesis

# Получить данные анамнеза
anamnesis = get_test_anamnesis("cardiovascular")
# Возвращает dict с полями: purpose, complaints, anamnesis
```

### Данные для медицинского отчёта

Три варианта готовых данных для создания/обновления медицинского отчёта:

- Вариант 1: Плановый осмотр
- Вариант 2: Консультация по гипертонии
- Вариант 3: Острая боль в животе

**Использование:**
```python
from tests.test_data import get_test_medical_report

report = get_test_medical_report(1)  # или 2, 3
```

### Данные артериального давления

Три варианта показателей давления для тестирования:

- Вариант 1: 140/90, пульс 75
- Вариант 2: 120/80, пульс 70
- Вариант 3: 160/100, пульс 88

**Использование:**
```python
from tests.test_data import get_test_blood_pressure

bp = get_test_blood_pressure(1)  # или 2, 3
```

## Тестовые сценарии

В файле `test_data.py` определены готовые сценарии тестирования:

1. **scenario_1_full_workflow** - Полный workflow: генерация → редактирование → извлечение анамнеза → сохранение → отправка в МИС
2. **scenario_2_transcription_editing** - Редактирование транскрипции с проверкой сохранения
3. **scenario_3_anamnesis_extraction** - Извлечение анамнеза из транскрипции
4. **scenario_4_blood_pressure** - Работа с показателями артериального давления
5. **scenario_5_pdf_generation** - Генерация PDF отчёта о приёме
6. **scenario_6_search_patients** - Поиск пациентов и приёмов

**Просмотр сценариев:**
```python
from tests.test_data import TEST_SCENARIOS

for name, scenario in TEST_SCENARIOS.items():
    print(f"{name}: {scenario['description']}")
    for step in scenario['steps']:
        print(f"  {step}")
```

## Pytest тесты

### Основные тесты функциональности (`test_functionality.py`)

Этот файл содержит полный набор pytest тестов для проверки работоспособности:

- **TestTranscriptionWorkflow** - тесты работы с транскрипцией
  - `test_update_transcription` - обновление транскрипции
  - `test_transcription_persistence` - сохранение при переключении
  - `test_different_transcription_scenarios` - различные сценарии (параметризованный)

- **TestAnamnesisExtraction** - тесты извлечения анамнеза (медленные, требуют OpenAI)
  - `test_extract_anamnesis` - извлечение анамнеза
  - `test_extract_anamnesis_different_scenarios` - различные сценарии

- **TestMedicalReport** - тесты работы с медицинскими отчётами
  - `test_create_medical_report` - создание отчёта (параметризованный)
  - `test_update_medical_report` - обновление отчёта
  - `test_get_medical_report` - получение отчёта
  - `test_submit_to_mis` - отправка в МИС

- **TestBloodPressure** - тесты работы с артериальным давлением
  - `test_update_blood_pressure` - сохранение давления (параметризованный)
  - `test_blood_pressure_persistence` - сохранение в портрете

- **TestPDFGeneration** - тесты генерации PDF
  - `test_generate_pdf` - генерация PDF отчёта

- **TestSearch** - тесты поиска
  - `test_search_patients` - поиск пациентов
  - `test_search_appointments` - поиск приёмов

- **TestEdgeCases** - тесты граничных случаев
  - `test_empty_transcription` - пустая транскрипция
  - `test_special_characters_transcription` - специальные символы
  - `test_unicode_transcription` - Unicode символы

- **TestErrorHandling** - тесты обработки ошибок
  - `test_invalid_appointment_id` - несуществующий приём
  - `test_invalid_audio_id` - несуществующий аудио
  - `test_invalid_patient_id` - несуществующий пациент
  - `test_invalid_blood_pressure` - некорректные значения давления

- **TestFullWorkflow** - тесты полного workflow (медленные)
  - `test_full_workflow_scenario` - полный цикл: транскрипция → анамнез → отчёт → МИС → PDF

### Быстрые тесты (`test_quick_check.py`)

Быстрые тесты для проверки работоспособности:

- `test_health_check` - проверка health check
- `test_appointments_exist` - наличие приёмов
- `test_patients_exist` - наличие пациентов
- `test_transcription_update_quick` - быстрое обновление транскрипции
- `test_medical_report_quick` - быстрое сохранение отчёта
- `test_pdf_generation_quick` - быстрая генерация PDF

**Запуск:**
```bash
# Все быстрые тесты
pytest tests/test_quick_check.py -v

# Все тесты функциональности
pytest tests/test_functionality.py -v

# Только быстрые (без медленных)
pytest tests/ -v -m "not slow"

# Только медленные тесты (требуют OpenAI API)
pytest tests/ -v -m "slow"

# Только API тесты
pytest tests/ -v -m "api"
```

### Маркеры pytest

Тесты используют следующие маркеры:

- `@pytest.mark.api` - API тесты (используются во всех тестах)
- `@pytest.mark.slow` - медленные тесты (требуют OpenAI API или долго выполняются)
- `@pytest.mark.parametrize` - параметризованные тесты (запускаются с разными данными)

**Примеры использования маркеров:**
```bash
# Пропустить медленные тесты
pytest tests/ -v -m "not slow"

# Запустить только медленные тесты
pytest tests/ -v -m "slow"

# Запустить только API тесты
pytest tests/ -v -m "api"
```

## Примеры использования

### Пример 1: Тестирование обновления транскрипции

```python
import httpx
from tests.test_data import get_test_transcription

async def test_update_transcription():
    async with httpx.AsyncClient(base_url="http://localhost:8000") as client:
        # Получаем существующий приём
        response = await client.get("/api/appointments")
        appointment_id = response.json()[0]["id"]
        
        # Получаем аудиофайл
        response = await client.get(f"/api/audio/by-appointment/{appointment_id}")
        audio_id = response.json()["id"]
        
        # Обновляем транскрипцию
        transcription = get_test_transcription("cardiovascular")
        response = await client.put(
            f"/api/audio/{audio_id}/transcription",
            json={"transcription_text": transcription}
        )
        
        assert response.status_code == 200
        print("✅ Транскрипция обновлена")
```

### Пример 2: Тестирование извлечения анамнеза

```python
import httpx

async def test_extract_anamnesis():
    async with httpx.AsyncClient(base_url="http://localhost:8000") as client:
        # Получаем приём и аудио
        response = await client.get("/api/appointments")
        appointment_id = response.json()[0]["id"]
        
        response = await client.get(f"/api/audio/by-appointment/{appointment_id}")
        audio_id = response.json()["id"]
        
        # Извлекаем анамнез
        response = await client.post(f"/api/audio/{audio_id}/extract-anamnesis")
        
        if response.status_code == 200:
            anamnesis = response.json()
            print(f"Цель: {anamnesis['purpose']}")
            print(f"Жалобы: {anamnesis['complaints']}")
            print(f"Анамнез: {anamnesis['anamnesis']}")
```

### Пример 3: Тестирование сохранения давления

```python
import httpx
from tests.test_data import get_test_blood_pressure

async def test_save_blood_pressure():
    async with httpx.AsyncClient(base_url="http://localhost:8000") as client:
        # Получаем пациента
        response = await client.get("/api/patients")
        patient_id = response.json()[0]["id"]
        
        # Сохраняем давление
        bp_data = get_test_blood_pressure(1)
        response = await client.post(
            f"/api/patients/{patient_id}/blood-pressure",
            json=bp_data
        )
        
        assert response.status_code == 200
        print("✅ Давление сохранено")
```

## Тестирование граничных случаев

В файле `test_data.py` определены тексты для проверки граничных случаев:

- Пустая транскрипция
- Очень длинный текст
- Специальные символы
- Unicode символы
- Многострочный текст
- Текст с числами и датами

**Использование:**
```python
from tests.test_data import EDGE_CASE_TEXTS

# Тест с очень длинным текстом
long_text = EDGE_CASE_TEXTS["very_long_transcription"]

# Тест с пустым текстом
empty_text = EDGE_CASE_TEXTS["empty_transcription"]
```

## Тестирование обработки ошибок

Определены тестовые случаи для проверки обработки ошибок:

- Несуществующий приём
- Несуществующий аудиофайл
- Несуществующий пациент
- Некорректные значения давления
- Пустой медицинский отчёт

**Использование:**
```python
from tests.test_data import ERROR_TEST_CASES

# Тест с несуществующим приёмом
invalid_id = ERROR_TEST_CASES["invalid_appointment_id"]["appointment_id"]
```

## Рекомендации по использованию

1. **Не создавайте новые приёмы** - используйте существующие данные из базы
2. **Проверяйте наличие данных** - перед тестированием убедитесь, что в БД есть приёмы и пациенты
3. **Используйте разные сценарии** - тестируйте различные медицинские случаи
4. **Проверяйте граничные случаи** - тестируйте обработку ошибок и нестандартных данных
5. **Восстанавливайте состояние** - после тестирования можно откатить изменения, если необходимо

## Запуск всех тестов

Для запуска всех тестов через pytest:

```bash
# Все тесты функциональности
pytest tests/test_functionality.py -v

# Быстрая проверка
pytest tests/test_quick_check.py -v

# Все тесты в директории tests/
pytest tests/ -v

# Только быстрые тесты (исключая медленные)
pytest tests/ -v -m "not slow"

# Только медленные тесты
pytest tests/ -v -m "slow"

# Конкретный класс тестов
pytest tests/test_functionality.py::TestTranscriptionWorkflow -v

# Конкретный тест
pytest tests/test_functionality.py::TestTranscriptionWorkflow::test_update_transcription -v

# С подробным выводом
pytest tests/test_functionality.py -v -s

# С покрытием кода (если установлен pytest-cov)
pytest tests/test_functionality.py --cov=app --cov-report=html
```

Для запуска примеров кода (не pytest тесты):

```bash
# Примеры использования
python tests/test_usage_examples.py

# Быстрая проверка (старый скрипт)
python tests/quick_test.py
```

## Структура тестовых данных

```
tests/
├── test_data.py              # Все тестовые данные
├── test_functionality.py      # Основные pytest тесты функциональности ⭐
├── test_quick_check.py        # Быстрые pytest тесты ⭐
├── test_usage_examples.py     # Примеры использования (не pytest)
├── quick_test.py              # Скрипт быстрой проверки (не pytest)
├── README_TEST_DATA.md        # Данная инструкция
├── test_api.py                # Существующие API тесты
└── test_e2e.py                # Существующие E2E тесты
```

⭐ - Рекомендуется использовать эти файлы для тестирования через pytest

## Примечания

- Все тестовые данные используют существующие записи в базе данных
- Новые приёмы не создаются (как и требовалось)
- Данные можно использовать для ручного тестирования через API или автоматических тестов
- Тестовые тексты имитируют реальные медицинские разговоры

## Дополнительная информация

Для получения дополнительной информации о структуре API см.:
- `app/api/` - эндпоинты API
- `app/schemas.py` - схемы данных
- `tests/test_api.py` - существующие API тесты

